/** @format */

import React from "react";

const Logout = () => {
    return (
        <div className="logout">
            <button id="logoutBtn">Logout</button>
        </div>
    );
};

export default Logout;
